[package]
name = "pill_game"
version = "0.1.0"
edition = "2021"
workspace = "NO_PATH"

[lib]
crate-type = ["cdylib"]

[dependencies]
pill_engine = {path = "../../crates/pill_engine", features = ["game"]}

