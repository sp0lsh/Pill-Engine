iname: Install Linux deps
description: Install native Linux packages required to build Pill-Engine examples
runs:
  using: "composite"
  steps:
    - shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y pkg-config libasound2-dev
